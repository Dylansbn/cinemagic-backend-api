# Utilise l'image Python officielle
→FROM python:3.10
# Définit l'environnement
WORKDIR /app

# Ligne originale: COPY requirements.txt .
COPY requirements-pro.txt .

# Ligne originale: RUN pip install --no-cache-dir -r requirements.txt
RUN pip install --no-cache-dir -r requirements-pro.txt

# Copie le reste du code de l'application
COPY . .

# Définit la commande de démarrage (utilise Gunicorn)
CMD ["gunicorn", "--bind", "0.0.0.0:8000", "server:app"]